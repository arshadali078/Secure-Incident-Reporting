# In dev we use Vite proxy, so API defaults to /api/v1
# For production, set VITE_API_URL to your backend URL + /api/v1
# Example: https://your-backend.onrender.com/api/v1
VITE_API_URL=http://localhost:5000/api/v1

# Socket server URL. In dev it can be empty to use same-origin.
VITE_SOCKET_URL=
